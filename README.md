# invoice_processing_aiagent
The projects is designed to do end to end invoice processing using agentic AI. It reads the mail, download invoice pdf, uses vision model to extract data and convert into a json. Once the invoice is validated and approved by Human in the Loop, the data is passed to finance service for processing. 
